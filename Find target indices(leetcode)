class Solution:
    def targetIndices(self, nums: List[int], target: int) -> List[int]:
        nums.sort()
        new = [idx for idx, num in enumerate(nums) if num == target]

        return new

"""
Time - O(N)
Space - O(1)
"""
